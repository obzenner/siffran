"""Empirica 2.0 black-box behavioral conformance tests.

The suite was introduced red-first before the v2 composition seam existed. It now binds to the real
``empirica/v2`` SUT through ``driver.new_driver`` and imports no core policy function or duplicate
runtime algorithm.
"""
